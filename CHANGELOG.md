
## 2026-07-18

- Added an explicit GitHub Pages deployment workflow that publishes the root static landing page artifact.
- Mirrored the landing page to `docs/index.html` so both root and `/docs` Pages source settings render the designed UI instead of the README title.
